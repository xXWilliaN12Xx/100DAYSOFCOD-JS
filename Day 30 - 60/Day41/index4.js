// Escreva uma função chamada ePaisagem que receba dois parametros, largura e altura de uma imagem (numero).
// Retorne true se a imagem estiver no modo paisagem.

let ePaisagem = (largura, altura) => {
    largura > altura
};

console.log(ePaisagem(1920, 1080));