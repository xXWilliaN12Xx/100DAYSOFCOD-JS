// Solicite a nota do usuário
let nota = Number(70);

// Verifique a classificação da nota

if (nota >= 90) {
  // calculando a nota.
  console.log("A"); // imprime o resultado no terminal.
} else if (nota >= 80) {
  // calcula a nota
  console.log("B"); // imprime o resultado no terminal.
} else if (nota >= 70) {
  // calcula a nota.
  console.log("C"); // imprime o resultado no terminal.
} else if (nota >= 60) {
  // calcula a nota.
  console.log("D"); // imprime o resultado no terminal.
} else {
  // finalização
  console.log("F"); // imprime o resultado no terminal.
}
