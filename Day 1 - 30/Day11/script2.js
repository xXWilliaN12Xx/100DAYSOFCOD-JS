let c = 1; // variavel criada com o valor 1;
do {
  // estrutura de repetição com teste lógico no final;
  console.log(`passo ${c}`); // valor é exebido no output;
  c++; // A variavel 'c' passa a receber 1 em 1;
} while (c <= 11);
